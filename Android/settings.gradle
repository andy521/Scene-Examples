rootProject.name = "SceneExample"
if (!isModule.toBoolean()) {
    include ':app'
}

include ':libs:base-library'
include ':libs:ui-widget'

include ':modules:SingleHostLive'
include ':modules:SuperApp'
